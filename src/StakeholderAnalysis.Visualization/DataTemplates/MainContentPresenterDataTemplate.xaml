<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewModels="clr-namespace:StakeholderAnalysis.Visualization.ViewModels"
                    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
                    xmlns:visualization="clr-namespace:StakeholderAnalysis.Visualization">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="OnionDiagramDataTemplate.xaml" />
        <ResourceDictionary Source="AttitudeImpactDiagramDataTemplate.xaml" />
        <ResourceDictionary Source="StakeholderForcesDataTemplate.xaml" />
        <ResourceDictionary Source="StakeholderTableDataTemplate.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Key="MainContentPresenterDataTemplate" DataType="viewModels:MainWindowViewModel">
        <ContentPresenter x:Name="Presenter" Content="{Binding SelectedViewInfo.ViewModel}">
            <ContentPresenter.Style>
                <Style TargetType="ContentPresenter">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSaveToImage}" Value="True">
                            <Setter Property="visualization:SaveToImageBehaviours.SaveToImage" Value="True" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSaveToImage}" Value="False">
                            <Setter Property="visualization:SaveToImageBehaviours.SaveToImage" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentPresenter.Style>
            <xctk:MagnifierManager.Magnifier>
                <xctk:Magnifier
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Radius="100"
                    ZoomFactor=".4"
                    Visibility="{Binding IsMagnifierActive, Converter={StaticResource BooleanToVisibilityConverter}}"
                    FrameType="Circle" />
            </xctk:MagnifierManager.Magnifier>
        </ContentPresenter>
        <DataTemplate.Triggers>
            <DataTrigger Binding="{Binding SelectedViewInfo.Type}" Value="{x:Static viewModels:StakeholderViewType.Onion}">
                <Setter TargetName="Presenter" Property="ContentTemplate" Value="{StaticResource OnionDiagramDataTemplate}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedViewInfo.Type}" Value="{x:Static viewModels:StakeholderViewType.AttitudeImpact}">
                <Setter TargetName="Presenter" Property="ContentTemplate" Value="{StaticResource AttitudeImpactDiagramDataTemplate}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedViewInfo.Type}" Value="{x:Static viewModels:StakeholderViewType.StakeholderForces}">
                <Setter TargetName="Presenter" Property="ContentTemplate" Value="{StaticResource StakeholderForcesDataTemplate}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding SelectedViewInfo.Type}" Value="{x:Static viewModels:StakeholderViewType.StakeholderTable}">
                <Setter TargetName="Presenter" Property="ContentTemplate" Value="{StaticResource StakeholderTableDataTemplate}" />
            </DataTrigger>
        </DataTemplate.Triggers>
    </DataTemplate>
</ResourceDictionary>